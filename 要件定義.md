# 書類自動整理アップローダー（MVP）要件定義

## 1. 目的・成功条件

### 目的
社内で扱う書類（MVPは請求書（発行）と領収書）を、アップロードするだけで自動で整理・タグ付けし、いつでも検索して取り出せる状態にする。

### 成功条件（MVP）
- 社員が **1件ずつDrag & Dropでアップロード** できる
- アップ後に **宛先・件名・発行日（請求書）** を確定できる（自動抽出＋補完入力）
- 保存された書類を **宛先×日付範囲×件名検索** で探せる
- **保存完了が担保**され、アップロード済み書類がロストしない（保存ステータス＋監査ログ）


## 2. スコープ

### 対象（MVP）
- ファイル形式：**PDFのみ**
- 書類種別：
  - **請求書（発行）**
  - **領収書**
- サイズ：**最大5MB**
- アップロード：**1件ずつ Drag & Drop**

### やらないこと（MVPでは除外）
- 画像（JPEG/PNG）/ Office（Word/Excel等）の対応
- PDF全文検索（本文インデックス）
- 自動承認ワークフロー（複数段階の承認）
- 会計ソフト連携、取引先マスタ連携
- 高度な学習（自動ルール学習の本格導入）


## 3. タグ仕様（検索キー）

### 3.1 請求書（発行）
必須タグ（確定値）：
- `doc_type`: `invoice_issued`
- `counterparty`（宛先）
- `subject`（件名：請求対象）
- `issue_date`（発行日：日付は1つに固定）

任意（取れたら保存）：
- `invoice_number`
- `amount`
- `currency`（基本JPY）

### 3.2 領収書
必須タグ（確定値）：
- `doc_type`: `receipt`
- `issuer`（発行元：店名/会社名）
- `receipt_date`（日付）
- `amount`（金額）


## 4. ユースケースとユーザーフロー

### 4.1 請求書（発行）登録フロー
1. ユーザーがPDFをDnD
2. システムが解析して候補抽出  
   - 宛先候補 / 件名候補 / 日付候補（複数ある場合あり）
3. UIで確認・補完  
   - 宛先：候補選択 or 手入力  
   - 件名：候補が弱ければ手入力  
   - 発行日：候補が複数なら選択
4. ユーザーが「確定して保存」
5. 保存成功（ステータス`stored`）→一覧・検索に出現

### 4.2 領収書登録フロー
同様に issuer/date/amount の候補抽出 → 未確定を補完 → 確定保存。

### 4.3 検索・閲覧
- フィルタ：`doc_type`、宛先（or issuer）、日付範囲（from-to）
- テキスト検索：件名（部分一致）
- 結果一覧から詳細→PDF表示/ダウンロード


## 5. 画面要件（MVP）

### 画面A：アップロード
- DnD領域
- バリデーション：PDFのみ、5MB以下
- 解析中ステータス表示（progress / spinner）
- 解析失敗時：理由表示＋再試行

### 画面B：タグ確認（確定画面）
- 抽出結果の表示（候補値＋信頼度）
- 宛先：候補選択（ドロップダウン）＋自由入力
- 件名：テキスト入力
- 発行日：日付選択（候補が複数なら候補リスト）
- 「確定して保存」ボタン
- ルール：必須タグが埋まらないと確定不可

### 画面C：一覧・検索
- フィルタ（doc_type、宛先/issuer、日付範囲）
- 件名検索ボックス
- 一覧（発行日、新しい順デフォルト）
- 行クリックで詳細へ

### 画面D：詳細
- タグ表示
- PDFプレビュー（可能なら埋め込み）またはダウンロード
- タグ修正（MVPでは“修正可”にすると運用が安定）
- 監査ログ（最低：作成者・作成日時・最終更新）


## 6. データモデル（たたき台）

### Document
- `id` (UUID)
- `doc_type` (enum)
- `original_filename`
- `mime_type`
- `size_bytes`
- `content_hash`（重複検出用・任意だが推奨）
- `storage_provider`（MVPは`s3`想定）
- `storage_key`（S3 object key）
- `storage_url`（署名URL生成は別途）
- `status`：`uploaded` / `parsed` / `needs_input` / `stored` / `failed`
- `created_by`（user id/email）
- `created_at`, `updated_at`

※MVPは `Document` にフラットにタグ項目を持たせてもよい（実装優先）

### ExtractionResult
- `document_id`
- `field_name`
- `candidate_value`
- `confidence`
- `source`（pdf_text / ocr）
- `created_at`

### AuditLog
- `document_id`
- `action`（upload/parse/confirm/edit/download）
- `actor`
- `timestamp`
- `metadata`


## 7. API要件（たたき台）
- `POST /documents/upload`  
  PDFアップロード → `document_id`返す
- `POST /documents/{id}/parse`  
  解析開始（upload直後に自動呼び出しでもOK）
- `GET /documents/{id}`  
  ドキュメント＋抽出候補＋確定タグ
- `POST /documents/{id}/confirm`  
  必須タグを受けて確定・保存（storedへ）
- `GET /documents`  
  検索（doc_type、counterparty/issuer、date range、subject query）
- `GET /documents/{id}/download`  
  署名URL発行（S3前提）


## 8. 非機能要件（MVP最小）

### セキュリティ
- 認証：社内SSO（将来）/ MVPは簡易でもOK（ただし限定公開）
- 監査：アップロード者、更新者、ダウンロードのログを残す

### 可用性・ロスト防止
- 「stored」になるまで完了扱いにしない
- DBにメタデータがあり、S3キーと紐づく
- 解析失敗・保存失敗時は `failed` で残し、再試行可能にする

### 性能
- 1件5MB、1件ずつなので同時数は低〜中で設計
- 解析は非同期ジョブ化が望ましい（MVPでもキューを入れると安定）


## 9. 保存ルール（S3想定の提案）
- Object Key例：  
  - 請求書：`invoices/issued/{YYYY}/{counterparty}/{issue_date}_{document_id}.pdf`
  - 領収書：`receipts/{YYYY}/{issuer}/{receipt_date}_{document_id}.pdf`
- `counterparty/issuer` は正規化（記号削除・空白統一など）してキー安全化


## 10. リスクと対策（MVPで潰すべきものだけ）
- PDFの文字が取れない（スキャン）
  - MVPは「抽出できなければ手入力」で成立。OCRは次フェーズで追加
- 宛先名の表記揺れ
  - MVPは手入力で確定し、将来「正規化辞書」へ
- 同じファイルの再アップ
  - content_hash があれば検出できる（任意だが推奨）